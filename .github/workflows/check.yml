name: Check

on:
  pull_request:

permissions:
  contents: read

jobs:
  # No AI attribution in commits or the pull request. Nothing else to build here.
  no-ai-signs:
    uses: CandleStack-FEI-STU/.github/.github/workflows/no-ai-signs.yml@main
